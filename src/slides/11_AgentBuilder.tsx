import React from 'react';
import type { SlideData } from '../../types';
import { AgentBuilderVisualGuide } from '../components/slides/AgentBuilderVisualGuide';

export const agentBuilderSlide: SlideData = {
    layout: 'custom',
    content: <AgentBuilderVisualGuide />
};
