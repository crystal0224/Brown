import React from 'react';
import type { SlideData } from '../../types';
import { KeywordCloud } from '../components/slides/KeywordCloud';

export const keywordsSlide: SlideData = {
    layout: 'custom',
    content: <KeywordCloud />
};
